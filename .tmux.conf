set-option -g history-limit 100000
set -g mouse on

# pass modified keys (shift+enter, ctrl+enter, ...) through to apps like Claude Code
set -s extended-keys on
set -as terminal-features 'xterm*:extkeys'
